
import 'package:flutter/material.dart';

class SensorLocationsPage extends StatelessWidget {
  const SensorLocationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sensor Locations')),
      body: const Center(child: Text('Sensor Locations Page')),
    );
  }
}